#include "./lib/utils.h"
#include <stdio.h>
#include <stdlib.h>

int alg(int arr[], int n, int key) {
  int res;
  return res;
}

int main() {
  int n = 25;
  int arr[n];
  generate(arr, 25);
  printIntArr(arr, 25);
}
